Description:
    Scaffolds an entire CMS object class, from model and migration to controller and views, along
    with a full test suite.

    Run `rails generate cms:model --help` to see all available options.

Example:
    `rails generate cms:scaffold News --attributes=headline:string max_results:string`
