.panel.panel-default
  .panel-heading
    %span.glyphicon.glyphicon-lock

    Welcome

    %em
      = current_user.contact.login

  .panel-body
    = link_to(cms_path(homepage.login_page), method: :delete, class: 'btn btn-danger') do
      Logout

  .panel-footer
    &nbsp;
