Description:
    Generates a language menu. It will link to the homepages below the current website. To create an
    example use "rails generate cms:component:language_switch:example".

Example:
    `rails generate cms:component:language_switch`
    `rails generate cms:component:language_switch:example`
