- if current_user.logged_in?
  = render('login_widget/views/logged_in', homepage: @obj.homepage, current_user: current_user)

- else
  = render('login_widget/views/logged_out', homepage: @obj.homepage, page: @obj)
